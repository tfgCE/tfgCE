#include "postProcessGraphProcessingContext.h"

//

using namespace Framework;

//

PostProcessGraphProcessingContext::PostProcessGraphProcessingContext()
: camera(nullptr)
, video3D(nullptr)
, renderTargetManager(nullptr)
, inputRenderTargetsProvider(nullptr)
, resolutionProvider(nullptr)
{
}
